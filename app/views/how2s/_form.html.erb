<%= form_for(@how2) do |f| %>
  <% if @how2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@how2.errors.count, "error") %> prohibited this list from being saved:</h2>

      <ul>
      <% @how2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
